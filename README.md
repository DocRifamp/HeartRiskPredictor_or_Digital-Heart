# 🩺 Heart Attack Risk Prediction API

## Обзор проекта

Этот проект реализует RESTful API на фреймворке **FastAPI** для предсказания риска сердечного приступа на основе данных пациента. API использует обученную модель **CatBoost** .

---

## Архитектура приложения

Проект  разделен на модули:

1. **`catboost_best_model.pkl`**: Бинарный файл, содержащий обученную модель CatBoost.
2. **`model_service.py`**: Класс `HeartRiskPredictor`, который инкапсулирует логику загрузки модели, предобработки данных (выбор 13 необходимых признаков, преобразование категориальных) и выполнения предсказания.
3. **`main.py`**: Основное приложение FastAPI. Оно настраивает логирование (`loguru`), регистрирует эндпоинты (`/predict`, `/model_info`) и реализует централизованную обработку ошибок.

---

## 🚀 Инструкция по запуску

### 1. Требования

Для запуска проекта необходим **Python 3.8+**.

### 2. Установка зависимостей

1. **Создайте виртуальное окружение** (рекомендуется):

    ```bash
    python -m venv venv
    source venv/bin/activate  # macOS/Linux
    .\venv\Scripts\Activate.ps1 # Windows PowerShell
    ```

2. **Установите необходимые библиотеки** из `requirements.txt`:

    ```bash
    pip install -r requirements.txt
    ```

### 3. Файлы для запуска

Перед запуском убедитесь, что в корневой папке проекта находятся следующие файлы:

* `main.py`
* `model_service.py`
* **`catboost_best_model.pkl`** (Ваша обученная модель)

### 4. Запуск сервера FastAPI

Запустите сервер с помощью Uvicorn в **Терминале 1**:

```bash
uvicorn main:app --reload
